import 'package:flutter/material.dart';

class SlidableDemo extends StatelessWidget {
  const SlidableDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Slidable Demo'),
      ),
      body: const Center(
        child: Text('Slidable Demo'),
      ),
    );
  }
}
