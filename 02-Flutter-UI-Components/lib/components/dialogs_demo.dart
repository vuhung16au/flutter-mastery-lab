import 'package:flutter/material.dart';

class DialogsDemo extends StatelessWidget {
  const DialogsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dialogs Demo'),
      ),
      body: const Center(
        child: Text('Dialogs Demo'),
      ),
    );
  }
}
