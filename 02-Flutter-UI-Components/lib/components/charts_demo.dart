import 'package:flutter/material.dart';

class ChartsDemo extends StatelessWidget {
  const ChartsDemo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Charts Demo'),
      ),
      body: const Center(
        child: Text('Charts Demo'),
      ),
    );
  }
}
